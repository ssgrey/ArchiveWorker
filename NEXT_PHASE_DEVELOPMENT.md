# 下一阶段开发任务书：真实图像清理引擎与批处理闭环

## 1. 新对话可直接执行的指令

请在当前仓库中继续开发“档案净页”WPF 程序，严格按照本文档完成下一阶段。不要只给方案，需要实际修改代码、运行样图、生成处理结果、补充测试，并启动程序验证真实预览。

本阶段的核心目标是：将当前静态红绿掩膜和演示分析流程替换为真实的离线图像处理，实现四边缺陷检测、内容保护冲突、局部背景修复、批量安全输出和审计记录。

开始前必须先阅读：

- `README.md`
- `ArchiveCleaner.Wpf/MainWindow.xaml`
- `ArchiveCleaner.Wpf/MainWindow.xaml.cs`
- `ArchiveCleaner.Wpf/Models/CleanupSettings.cs`
- `ArchiveCleaner.Wpf/Models/ArchiveImageItem.cs`
- `ArchiveCleaner.Wpf/ViewModels/MainViewModel.cs`
- `ArchiveCleaner.Wpf/Services/ImageCatalogService.cs`
- `Files/` 中的 5 张真实样图

完成后必须满足本文档第 12 节的验收标准。

---

## 2. 当前项目状态

技术栈：

- .NET 8 WPF
- 原生 WPF 控件，无第三方 UI 框架
- MVVM 风格的数据绑定，部分窗口行为在 Code-behind 中
- 样图会复制到程序输出目录的 `Files` 文件夹

当前已经实现：

- 加载图片和图片文件夹
- 文件队列、缩略图、原图双栏预览
- 四边候选范围设置
- 缺陷检测、内容保护、修复和输出参数界面
- 50 个详细 Tooltip
- JSON 设置方案保存
- 演示用批次分析进度及风险状态
- 保留区域、确认去除的交互入口

当前尚未实现：

- 真实 OpenCV 缺陷检测
- 真实内容保护掩膜
- 真实图片修复和处理前后预览
- 批量结果写出
- 取消、错误隔离、临时文件安全提交
- 文件哈希和审计报告
- ONNX 内容检测模型
- 人工笔刷实际绘制

注意：当前 XAML 中的红色椭圆、绿色正文框、修复色带和黄色提示均为静态演示元素，必须由真实分析结果替换。

---

## 3. 本阶段目标

完成一个可以用 `Files` 样图真实运行的离线 MVP：

1. 在左、右、上、下四个独立候选区域检测黑边、黑斑、孔洞和撕裂阴影。
2. 生成缺陷掩膜、内容保护掩膜和冲突掩膜。
3. 只自动修复高置信度缺陷。
4. 缺陷靠近疑似文字、批注、印章、表格线或页码时，不自动修复，标记为待复核。
5. 在 WPF 中显示真实的红、绿、黄半透明覆盖层。
6. 右侧预览必须显示算法实际生成的处理结果，不再显示静态色带。
7. 批量处理写入新目录，原文件永不覆盖。
8. 输出逐页审计信息和批次报告。
9. 所有处理完全离线，不调用网络服务。

安全优先级：

```text
保护档案内容 > 标记人工复核 > 自动清除干净程度 > 处理速度
```

宁可漏掉污点，也不能为了视觉干净而删除不确定内容。

---

## 4. 建议项目结构

在解决方案中新增核心类库和测试项目，使算法与 WPF 解耦：

```text
ArchiveWorker.sln
├─ ArchiveCleaner.Wpf
│  ├─ Models
│  ├─ Services
│  ├─ ViewModels
│  └─ Views / Controls（需要时再增加）
├─ ArchiveCleaner.Core
│  ├─ Contracts
│  ├─ Detection
│  ├─ Protection
│  ├─ Repair
│  ├─ Imaging
│  ├─ Batch
│  └─ Audit
└─ ArchiveCleaner.Core.Tests
```

建议 NuGet 依赖：

- `OpenCvSharp4`
- `OpenCvSharp4.runtime.win`
- 测试框架使用仓库中新增测试项目的标准选择，例如 xUnit

不要在核心类库中引用 WPF 类型。核心层返回普通数据对象和像素缓冲；WPF 层负责转换为 `BitmapSource`。

---

## 5. 核心接口与数据结构

接口名称可以结合代码风格调整，但职责必须保留。

### 5.1 清理引擎

```csharp
public interface IDocumentCleanupEngine
{
    Task<CleanupAnalysisResult> AnalyzeAsync(
        CleanupRequest request,
        IProgress<ProcessingProgress>? progress,
        CancellationToken cancellationToken);

    Task<CleanupResult> RepairAsync(
        CleanupAnalysisResult analysis,
        ReviewDecisionSet decisions,
        CancellationToken cancellationToken);
}
```

### 5.2 内容保护检测器

```csharp
public interface IContentProtectionDetector
{
    ContentProtectionResult Detect(
        ImageBuffer source,
        CandidateEdgeRegions edgeRegions,
        CleanupSettingsSnapshot settings,
        CancellationToken cancellationToken);
}
```

首个实现使用 OpenCV 保守规则，名称可为 `OpenCvContentProtectionDetector`。

同时预留 `OnnxContentProtectionDetector`，但如果仓库中没有经过确认的模型文件，不得下载来源不明的模型，也不得假装已经实现 ONNX 推理。此时应保留接口、模型目录约定和清晰状态说明。

### 5.3 分析结果至少包含

- 原图尺寸、DPI、方向
- 四边实际候选区域
- 缺陷掩膜
- 内容保护掩膜
- 冲突掩膜
- 每个连通缺陷区域的位置、面积、来源边、置信度和判定原因
- 高置信度自动清理区域
- 需要人工确认的区域
- 带覆盖层的预览图
- 分析耗时及警告信息

建议缺陷类型：

```text
PageEdgeShadow
BindingHole
DarkStain
TearShadow
UnknownDarkRegion
```

建议区域状态：

```text
AutoRemove
Protected
NeedsReview
Ignored
```

---

## 6. 图像处理流水线

### 6.1 图片读取

- 支持 JPG、JPEG、PNG、TIF、TIFF、BMP。
- 保留像素宽高、DPI 和文件扩展名。
- OpenCV 内部统一使用 BGR 或 BGRA。
- 分析阶段可以生成降采样副本，但最终掩膜和修复必须映射回原始分辨率。
- DPI 无效或为 0 时，使用 300 DPI 作为候选范围换算的临时默认值，并写入审计警告。
- 读取文件时使用允许其他进程读取的共享方式，不长时间锁定源文件。

毫米转像素：

```text
pixels = round(mm / 25.4 × dpi)
```

所有换算结果必须限制在图像尺寸以内。

### 6.2 页面方向与候选区域

- 四个方向分别根据 `CleanLeft/CleanRight/CleanTop/CleanBottom` 启用。
- 每个方向使用自己的毫米深度。
- 候选区域只是允许检测的范围，绝不能整块清空。
- 同一像素同时落入两个边缘候选区域时只处理一次。
- 当前版本始终保留原图方向，不自动旋转输出；候选范围按图片当前的上下左右处理。

### 6.3 纸张背景估计

- 转为 Lab 和灰度空间。
- 从候选区域内部及其邻近的干净带估计局部纸张亮度和颜色。
- 使用中位数、分位数或低频模糊背景，避免正文黑字影响背景值。
- 不得使用单一固定阈值处理所有页面。
- 泛黄、亮度不均匀和轻微纸纹不能直接判为污渍。

### 6.4 缺陷候选检测

组合以下信号：

- 相对局部纸张背景明显偏暗
- 与页面最外边缘相连的连续黑边或阴影
- 连通区域面积、宽高、紧致度、圆度和边界距离
- 装订孔常见的暗核心和周围撕裂阴影
- 同一批次中位置重复的缺陷
- 候选区域内不规则孤立深色块

推荐步骤：

1. 灰度和 Lab 亮度归一化。
2. 自适应阈值与局部背景差分并行生成暗像素候选。
3. 小尺度开运算去除孤立噪声。
4. 闭运算连接孔洞和撕裂边缘。
5. 连通区域分析。
6. 基于形状、边缘距离、背景差异和批次重复性计算置信度。

`DetectionSensitivity` 必须真实参与阈值或置信度计算，不能只停留在界面绑定。

`MaskExpansionPixels` 只对最终确认的缺陷区域膨胀，不得先对所有暗像素无条件扩张。

### 6.5 内容保护掩膜

首阶段使用保守的传统图像规则：

- 文字和手写笔画：保护细长笔画、相邻字符组和具有文本行排列特征的连通区域。
- 印章和签名：保护高饱和红、蓝区域；黑色印章无法确定时进入复核。
- 表格和页码：保护长直线、规则线框、页脚附近的小型字符组。
- 用户设置关闭某类保护时，只关闭相应规则，其他保护仍生效。
- 对保护掩膜按 `ProtectionPaddingPixels` 外扩。

不得声称传统规则能 100% 区分黑色手写笔画和黑色污斑。无法可靠区分时必须产生黄色冲突区域。

最终自动清理掩膜：

```text
AutoRemoveMask = HighConfidenceDefectMask - DilatedProtectionMask
```

冲突掩膜：

```text
ConflictMask = DefectCandidateMask ∩ DilatedProtectionMask
```

当 `BlockOnConflict` 开启时，所有冲突区域必须从自动清理掩膜移除，并标记 `NeedsReview`。

### 6.6 批次重复缺陷

当 `DetectRepeatedDefects` 开启：

- 按页面宽高归一化缺陷中心点和尺寸。
- 对同批页面中相近位置、相近形状的缺陷聚类。
- 重复出现只能提高“可能是装订/扫描缺陷”的置信度，不能绕过内容保护。
- 批次图片尺寸或方向差异明显时，自动降低该信号权重并记录警告。

### 6.7 背景修复

根据区域大小选择方法：

- 小型黑点和细裂纹：OpenCV Telea inpaint。
- 中型孔洞和撕裂：从缺陷靠页面内部一侧采样局部纸张颜色，再结合低频纹理重建。
- 大型连续扫描黑边：按行或列从相邻干净纸张带估计背景，进行渐变填充。
- 使用羽化掩膜融合修复边缘。

禁止：

- 整个候选边距填纯白
- 使用生成式模型臆造文字或档案内容
- 修复受保护区域
- 在没有人工确认时修复黄色冲突区域

`RepairMethod` 和 `ColorFeathering` 必须影响真实修复行为。

---

## 7. WPF 集成要求

### 7.1 替换静态演示覆盖层

删除或隐藏 `MainWindow.xaml` 中固定位置的：

- 三个红色椭圆
- 固定绿色正文保护框
- 固定 20 mm 竖线位置
- 固定修复色带
- 固定黄色风险标签

改为根据 `CleanupAnalysisResult` 动态绘制。

建议使用覆盖在 `Image` 上方的 `Canvas`：

- 红色：`AutoRemove`
- 绿色：`Protected`
- 黄色：`NeedsReview`
- 坐标从原始图片像素映射到当前预览尺寸
- 图片缩放、窗口调整和切换文件后覆盖层必须保持对齐

如果逐像素掩膜区域很多，优先将掩膜合成为透明 `BitmapSource`，不要创建数万个 WPF Shape。

### 7.2 真实处理预览

- 左侧显示原图加真实分析掩膜。
- 右侧显示实际修复后的 `BitmapSource`。
- 切换文件时按需加载缓存结果。
- 参数变化后不自动频繁运行昂贵算法；点击“应用设置并重新预览”后重新分析。
- 分析期间显示忙碌状态，禁用会引发重复分析的按钮。
- 支持取消当前分析或批处理。

### 7.3 风险复核

- 文件队列显示每页真实状态：等待、分析中、完成、需要确认、失败。
- “保留该区域”将对应缺陷区域记录为 `Protected`。
- “确认去除”将对应区域记录为人工批准，之后允许修复。
- 人工决定必须按文件和区域 ID 保存，重新预览后不能丢失。
- 当前页包含多个冲突区域时，需要能够逐个选择，不能用一个按钮无差别批准全部区域。

人工笔刷完整绘制可以留到再下一阶段，但本阶段至少要完成按区域的保留/去除决定。

### 7.4 Tooltip

- 新增的设置、按钮、风险操作和输出设置必须继续提供详细、通俗 Tooltip。
- Tooltip 至少说明：作用、开启或调大后的影响、风险和推荐使用场景。
- 不得删除现有 Tooltip。

---

## 8. 批量处理和输出安全

### 8.1 输出目录

- 增加输出文件夹选择。
- 默认输出到源目录同级的新文件夹，例如 `ArchiveCleaner_Output_yyyyMMdd_HHmmss`。
- 禁止输出目录与源目录完全相同。
- 禁止覆盖源文件。
- 同名结果已存在时，默认追加序号或提示用户选择策略。

### 8.2 安全写入

每个文件必须：

1. 在输出目录创建唯一临时文件。
2. 完整编码并关闭文件流。
3. 验证结果可重新解码、宽高正确且文件非空。
4. 再将临时文件原子重命名为最终文件名。
5. 失败时删除临时文件，保留其他已成功结果。

不得先删除已有结果文件后再写。

### 8.3 图片编码

- 保持原始像素宽高。
- `PreserveDimensionsAndDpi` 开启时保留 DPI。
- `OutputFormat=与原图一致` 时按源扩展名选择编码器。
- JPEG 使用 `JpegQuality`。
- PNG 使用无损编码。
- TIFF 首阶段支持单页 TIFF；遇到多页 TIFF 时必须明确提示“不支持多页 TIFF”，不得只处理第一页后静默覆盖。

### 8.4 并发和内存

- 使用可取消的异步批处理。
- 并发数默认不超过 `min(4, max(1, CPU 核数 / 2))`。
- 300 DPI 页面在 OpenCV 中可能同时存在多个 Mat，处理完成后及时释放。
- 不要一次把整个大批次的全分辨率图片全部载入内存。
- 单张失败不能终止整个批次，除非是输出目录不可写等全局错误。

---

## 9. 审计记录

输出目录至少生成：

```text
batch-report.json
batch-report.csv
```

逐页记录：

- 源文件绝对路径或批次内相对路径
- 输出文件路径
- 源文件 SHA-256
- 输出文件 SHA-256
- 源图和输出图尺寸、DPI、格式
- 使用的设置快照
- 自动清理区域数量和像素面积
- 受保护区域数量
- 待复核区域数量
- 人工保留/批准记录
- 处理开始时间、结束时间和耗时
- 成功、跳过或失败状态
- 错误信息和警告

JSON 使用 UTF-8；CSV 应正确转义中文、逗号、引号和换行。

---

## 10. 参数校验

在界面输入和核心层入口都要校验：

- 四边范围：每侧为非负值，实际候选区域不超过对应图片宽度或高度的 `50%`
- 检测灵敏度：`0～100`
- 缺陷掩膜扩展：`0～30 px`
- 保护区域外扩：`0～50 px`
- 颜色羽化：`0～100`
- JPEG 质量：`70～100`

非法值不能进入算法。界面应给出通俗错误提示，而不是抛出未处理异常。

核心层不能只依赖 UI 校验，因为设置可能来自 JSON 文件。

---

## 11. 测试要求

### 11.1 单元测试

至少覆盖：

- 毫米到像素换算，包括无效 DPI
- 四边候选区域的坐标和边界裁剪
- 启用/禁用各边后的掩膜范围
- 删除掩膜不越过候选区域
- 删除掩膜排除保护掩膜
- 冲突区域在 `BlockOnConflict=true` 时不进入自动修复
- 参数校验
- 输出文件命名不覆盖源文件
- SHA-256 计算
- CSV 转义

### 11.2 图像回归测试

使用 `Files` 中真实样图建立测试，但不要把输出结果覆盖回 `Files`。

每张样图至少验证：

- 输出尺寸与原图一致
- 输出可以重新解码
- 原图没有变化，源文件哈希保持不变
- 自动删除掩膜只出现在启用的四边候选范围内
- 原始像素缓冲中，最终修复掩膜之外的像素不被算法修改
- 页码和正文保护区域不进入自动删除掩膜

JPEG 重新编码会改变修复区域之外的压缩像素，因此“掩膜外像素不变”的断言应在编码前的原始像素缓冲上执行；不能对重新编码后的 JPEG 做逐像素完全相等要求。

### 11.3 运行验证

必须实际执行：

```powershell
dotnet build .\ArchiveWorker.sln -c Release
dotnet test .\ArchiveWorker.sln -c Release
```

并启动 WPF 程序验证：

- 5 张样图全部加载
- 点击分析后出现真实掩膜
- 切换文件掩膜与图片对齐
- 调整左侧范围后重新预览，检测区域发生真实变化
- 关闭某一方向后该方向不再产生删除候选
- 批量输出生成新目录和审计报告
- 关闭或取消处理中程序不会留下损坏的最终文件

保存一张真实运行截图到 `Design/archive-cleaner-wpf-engine-running.png`。

---

## 12. 完成验收标准

只有同时满足以下条件，才能将本阶段报告为完成：

1. 静态演示掩膜已替换为真实算法结果。
2. `Files` 中的样图可以完成真实分析和修复预览。
3. 四边开关和毫米范围真实影响算法。
4. 内容保护掩膜真实参与最终删除掩膜计算。
5. 冲突区域不会在未确认时自动修复。
6. 至少可以逐个对冲突区域选择“保留”或“确认去除”。
7. 批量处理可以写入新目录，且原图哈希不变。
8. 输出图片尺寸、DPI 和格式符合设置。
9. 生成 JSON 和 CSV 审计报告。
10. 批处理支持进度、取消和单文件错误隔离。
11. 核心算法具有自动化测试。
12. Release 构建和测试全部通过，无未处理运行时异常。
13. WPF 实际运行截图通过检查，无控件重叠，掩膜与图片对齐。
14. README 更新为真实功能状态，不再把已完成能力描述为“待接入”。

---

## 13. 本阶段非目标

以下内容可以继续预留接口，但不要为了扩大范围阻碍本阶段闭环：

- OCR 文字内容识别
- 搜索、索引或 PDF 生成
- 生成式 AI 图像修复
- 云端 API
- 用户权限和服务器数据库
- 完整的自由绘制笔刷系统
- 多页 TIFF 的完整编辑
- 未经确认的第三方 ONNX 模型下载
- 对所有历史档案保证 100% 自动区分手写内容与污渍

---

## 14. ONNX 后续接口约定

本阶段应该预留 ONNX 内容检测能力，但模型接入需要单独满足：

- 模型来源、许可证和版本明确
- 模型文件可随程序离线部署
- 输入尺寸、归一化、输出张量和后处理有文档
- 针对中文印刷文字、手写批注和印章分别验证
- 模型缺失或加载失败时，程序退回 OpenCV 保守规则并在状态栏明确提示
- 模型结果只能增强保护掩膜，不能绕过保护规则直接批准删除

建议模型目录：

```text
ArchiveCleaner.Wpf/Models/Onnx/
```

不要将大模型文件直接提交到仓库，除非用户明确要求并确认许可证及分发方式。

---

## 15. 最终交付说明格式

新对话完成工作后，最终回复必须简要列出：

- 实际实现的算法和安全规则
- 新增或修改的主要项目与文件
- 5 张样图的处理结果摘要
- 输出目录和运行截图路径
- 构建、测试结果
- ONNX 是否实际接入；若未接入，必须明确说明原因和回退行为
- 仍存在的误判风险和下一步建议

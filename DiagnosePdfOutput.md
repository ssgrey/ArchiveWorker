# PDF输出诊断指南

## 问题现象
输出的PDF和原始文件看起来没有变化

## 根本原因
黄色"待复核"区域默认**不会被处理**，只有以下区域会被处理：
- ✅ 红色区域（自动清除）
- ✅ 绿色区域（内容保护）
- ❌ 黄色区域（待复核）- **需要手动确认**

## 检查步骤

### 1. 查看分析结果
加载PDF后，点击"分析当前批次"，观察：
- 红色区域数量（会自动处理）
- 黄色区域数量（需要确认）
- 界面上的提示信息

### 2. 查看风险面板
右侧风险面板会显示：
```
当前批次共有 X 个待复核区域需要人工确认
```

如果X > 0，说明有黄色区域未确认

### 3. 确认处理方式

**选项A：批量确认（快速）**
1. 点击"批量确认去除所有待复核区域"按钮
2. 等待处理完成
3. 再点击"导出结果"

**选项B：逐个确认（精确）**
1. 在左侧列表选择每个图片/页面
2. 查看中间预览区域的黄色标记
3. 对每个黄色区域点击"确认去除"或"保留"
4. 完成后点击"导出结果"

**选项C：调整参数（减少黄色区域）**
1. 调整"总灵敏度"滑块（降低会减少检测到的缺陷）
2. 展开"高级检测参数"
3. 调整"自动清除门槛"（降低会让更多区域自动清除而不需要复核）
4. 重新点击"分析当前批次"
5. 点击"导出结果"

## 技术细节

### 代码逻辑
```csharp
// MainViewModel.cs:1048
private static ReviewDecisionSet CreateDecisionSet(ArchiveImageItem item) =>
    new(item.Decisions, ...);
```

`item.Decisions` 只包含用户已确认的区域，未确认的黄色区域不在其中。

### 处理流程
1. 分析阶段：检测缺陷，分类为红/绿/黄
2. 复核阶段：用户确认黄色区域（可选）
3. 导出阶段：只处理红色区域 + 已确认的黄色区域

### AssemblePdfOutputsAsync逻辑
```csharp
// 第841行：选择使用处理后的文件还是原始文件
var outputPagePaths = pages.Select((page, index) => 
    records[index]?.Status == BatchItemStatus.Success 
        ? records[index]!.OutputPath!  // 处理后的文件
        : page.FilePath                 // 原始栅格化的PNG
).ToArray();
```

如果页面处理成功但没有实际修改（因为黄色区域未确认），输出会和原始看起来相同。

## 预期行为

这是**设计行为**，不是bug：
- 黄色区域表示"程序不确定是否应该清除"
- 可能是黑色手写笔画 vs 黑色污渍
- 可能是重要内容边缘 vs 阴影
- 需要人工判断才能安全处理

## 快速解决

如果您确信黄色区域都应该被清除：
1. 点击"批量确认去除所有待复核区域"
2. 点击"导出结果"
3. 完成！
